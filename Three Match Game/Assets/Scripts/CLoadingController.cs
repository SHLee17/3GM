using UnityEngine;
using UnityEngine.UI;

public class CLoadingController : MonoBehaviour
{
    static string nextScene;

    Image image;
    // Start is called before the first frame update
    void Start()
    {
        
    }
}
